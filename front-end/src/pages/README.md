# File-based routing

Vue files in this directory become public routes. Keep developer documentation in Markdown files so it is not published as a page.

The `~/` and `@/` aliases resolve to `src/`. See the [Vue Router documentation](https://router.vuejs.org/guide/advanced/typed-routes.html) for typed routes.
